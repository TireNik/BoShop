spring:

  data:
    mongodb:
      uri: mongodb://localhost:27017/test_product_db

  kafka:
    bootstrap-servers: localhost:0000
    listener:
      auto-startup: false
      missing-topics-fatal: false
    properties:
      schema.registry.url: http://localhost:0000